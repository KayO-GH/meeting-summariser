[project]
name = "meeting-summariser"
version = "0.1.0"
description = "AI meeting summariser sample app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "google-genai>=1.73.1",
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
]
